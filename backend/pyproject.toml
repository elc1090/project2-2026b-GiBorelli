[project]
name = "backend"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "sqlalchemy>=2.0.0",
    "python-dotenv>=1.0.0",
    "psycopg[binary]>=3.3.5",
]

[tool.fastapi]
entrypoint = "main:app"
